version: "3.9"
services:
  app:
    image: tomyj/tank-conqueror
    build:
      context: .
      dockerfile: app.Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - type: bind
        source: .
        target: /app
    networks:
      - backend
networks:
  backend:
    name: backend